{
  "name": "ai-types-confusing",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/ai-types": "^0.0.7",
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "typescript": "^5.5.2",
    "vitest": "2.1.9",
    "wrangler": "^3.60.3"
  }
}
